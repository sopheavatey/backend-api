# Use a lightweight Python base image
FROM python:3.10-slim

# Set the working directory inside the container
WORKDIR /app

# 1. Install System Dependencies (CRITICAL for YOLO/OpenCV)
# OpenCV needs these graphics libraries to run, even without a monitor.
RUN apt-get update && apt-get install -y \
    libgl1-mesa-glx \
    libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

# 2. Copy ONLY requirements.txt first
COPY requirements.txt .

# 3. Install Python dependencies
# --no-cache-dir keeps the image smaller
RUN pip install --no-cache-dir -r requirements.txt

# 4. NOW copy the rest of your source code
COPY . .

# 5. The command to start your app
# Adjust 'main:app' to match your FastAPI/Flask file name
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]